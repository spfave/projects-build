{
	"name": "@projectsbuild/root",
	"version": "0.0.0",
	"type": "module",
	"description": "",
	"keywords": [],
	"author": "Sebastian",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/spfave/projects-build"
	},
	"engines": {
		"node": ">=24.0.0",
		"pnpm": ">=10.0.0"
	},
	"scripts": {
		"start": "pnpm -r --parallel run start",
		"format": "pnpm -r --no-bail --stream run format",
		"lint": "pnpm -r --no-bail --stream run lint",
		"typecheck": "pnpm -r --no-bail --parallel run typecheck",
		"clean:deps": "find ./ -name node_modules -type d -exec rm -rf {} +"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.12"
	}
}
